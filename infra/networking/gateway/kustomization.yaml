apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:

  # Gateway resources
  - gw-class.yaml
  - ns.yaml
  - gw-external.yaml
  - gw-internal.yaml
  - cert-drogontech.yaml


generatorOptions:
  disableNameSuffixHash: true