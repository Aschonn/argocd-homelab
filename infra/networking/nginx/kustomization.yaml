apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nginx

helmCharts:
  - name: ingress-nginx
    repo: https://kubernetes.github.io/ingress-nginx
    version: 4.14.1
    releaseName: nginx
    includeCRDs: true
    valuesInline:
      controller:
        replicaCount: 1
        service:
          type: LoadBalancer # so it accepts requests from cidr list from cilium ip pool (mine was set at 192.168.0.225)
        ingressClassResource:
          default: true
      defaultBackend:
        enabled: false
